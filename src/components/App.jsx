import { MainContainer } from './App.styled';
import { UsersList } from './UsersList/UsersList';

export const App = () => {
  return (
    <MainContainer>
      <UsersList />
    </MainContainer>
  );
};
